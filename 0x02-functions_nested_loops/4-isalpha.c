#include "main.h"

/**
 * _islower - Checks for lowercase Charater
 * gc: The Charater to be checked
 * Return: 1 for lowercase character or 0 anything else
 */

int _isalpha(int c)

{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
	{

	return (1);

	}

	return (0);
}
